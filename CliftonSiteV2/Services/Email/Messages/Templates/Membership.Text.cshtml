@using RazorLight
@using CliftonSiteV2.Models
@inherits TemplatePage<EmailMessage>

Membership question from the website

We have received the following membership query from the website.

@{ await IncludeAsync($"Services.Email.Messages.Templates.Partials._{Model.GetType().Name}Details.Text.cshtml", Model); }
